enum TableName {
  USERS = 'users',
  EPISODES = 'episodes',
  SHOW_NOTES = 'show_notes',
  MIGRATIONS = 'migrations',
  PODCASTS = 'podcasts',
  COMMENTS = 'comments',
  RECORDS = 'records',
  IMAGES = 'images',
}

export { TableName };
