enum TableName {
  USERS = 'users',
  EPISODES = 'episodes',
  MIGRATIONS = 'migrations',
  PODCASTS = 'podcasts',
  COMMENTS = 'comments',
  RECORDS = 'records',
  IMAGES = 'images',
}

export { TableName };
