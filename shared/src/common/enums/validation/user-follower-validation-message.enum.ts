const UserFollowerValidationMessage = {
  USER_ID_REQUIRE: 'User id required',
  USER_ID_NUMBER_FORMAT: 'User id must be an integer',
  FOLLOWER_ID_REQUIRE: 'Follower id required',
  FOLLOWER_ID_NUMBER_FORMAT: 'Follower id must be an integer',
} as const;

export { UserFollowerValidationMessage };
