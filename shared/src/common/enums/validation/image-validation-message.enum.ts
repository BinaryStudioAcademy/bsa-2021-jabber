const ImageValidationMessage = {
  DATA_URL_FORMAT: 'Cover image must be valid jpeg, png or svg file',
  USER_ID_REQUIRE: 'User id required',
  USER_ID_NUMBER_FORMAT: 'User id must be an integer',
} as const;

export { ImageValidationMessage };
