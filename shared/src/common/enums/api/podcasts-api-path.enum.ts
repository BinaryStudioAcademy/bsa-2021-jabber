enum PodcastsApiPath {
  ROOT = '/',
  $ID = '/:id',
  SEARCH = '/search',
  USERS = '/users',
  USERS_$ID = '/users/:id',
  INVITE = '/invite',
  INVITE_$CODE = '/invite/:code',
}

export { PodcastsApiPath };
