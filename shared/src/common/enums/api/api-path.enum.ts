enum ApiPath {
  AUTH = '/auth',
  USERS = '/users',
  PODCASTS = '/podcasts',
  EPISODES = '/episodes',
  COMMENTS = '/comments',
  RECORDS = '/comments',
  IMAGES = '/images',
}

export { ApiPath };
